name: 'ethiopian_medical_dbt'
version: '1.0'
profile: 'ethiopian_medical_dbt'

config-version: 2

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"
